diff --git a/kernel/runtime/ksud.c b/kernel/runtime/ksud.c
--- a/kernel/runtime/ksud.c
+++ b/kernel/runtime/ksud.c
@@ -54,5 +54,7 @@ void on_post_fs_data(void)
     ksu_observer_init();
     // sanity check, this may influence the performance
     stop_input_hook();
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     ksu_selinux_hide_handle_post_fs_data();
+#endif
 }
@@ -216,7 +218,9 @@ int ksu_handle_execveat_ksud(int *fd, struct filename **filename_ptr,
             char buf[16];
             if (check_argv(*argv, 1, "second_stage", buf, sizeof(buf))) {
                 pr_info("/system/bin/init second_stage executed\n");
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
                 ksu_selinux_hide_handle_second_stage();
+#endif
                 apply_kernelsu_rules();
                 cache_sid();
                 setup_ksu_cred();
@@ -288,7 +292,9 @@ int ksu_handle_execveat_ksud(int *fd, struct filename **filename_ptr, struct user
         char buf[16];
         if (!init_second_stage_executed && check_argv(*argv, 1, "second_stage", buf, sizeof(buf))) {
             pr_info("/system/bin/init second_stage executed\n");
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
             ksu_selinux_hide_handle_second_stage();
+#endif
             apply_kernelsu_rules();
             cache_sid();
             setup_ksu_cred();
diff --git a/kernel/policy/allowlist.c b/kernel/policy/allowlist.c
--- a/kernel/policy/allowlist.c
+++ b/kernel/policy/allowlist.c
@@ -448,6 +448,6 @@ static void migrate_profile(u32 version, struct app_profile *profile)
             }
         }
-        fallthrough;
+        /* fall through */
     case 3:
         if (profile->allow_su) {
             profile->rp_config.profile.flags = FLAG_KSU_NO_NEW_PRIVS;
diff --git a/kernel/sulog/event.c b/kernel/sulog/event.c
--- a/kernel/sulog/event.c
+++ b/kernel/sulog/event.c
@@ -142,7 +142,7 @@ static __u32 ksu_sulog_flatten_argv(struct user_arg_ptr *argv_user, char *dst, __u32 dst_len)
         if (IS_ERR(arg_user))
             return ksu_sulog_copy_empty_string(dst);

         copied =
-            strncpy_from_user_nofault(arg, (const void __user *)untagged_addr((unsigned long)arg_user), sizeof(arg));
+            ksu_strncpy_from_user_nofault(arg, (const void __user *)untagged_addr((unsigned long)arg_user), sizeof(arg));
         if (copied <= 0)
             return ksu_sulog_copy_empty_string(dst);
diff --git a/kernel/kpm/kpm.c b/kernel/kpm/kpm.c
--- a/kernel/kpm/kpm.c
+++ b/kernel/kpm/kpm.c
@@ -135,2 +135,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg1, 255)) {
+        if (!ksu_access_ok(arg1, 255)) {
             goto invalid_arg;
@@ -142,2 +142,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-            if (!access_ok(arg2, 255)) {
+            if (!ksu_access_ok(arg2, 255)) {
                 goto invalid_arg;
@@ -161,2 +161,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg1, sizeof(kernel_name_buffer))) {
+        if (!ksu_access_ok(arg1, sizeof(kernel_name_buffer))) {
             goto invalid_arg;
@@ -181,2 +181,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg1, sizeof(kernel_name_buffer))) {
+        if (!ksu_access_ok(arg1, sizeof(kernel_name_buffer))) {
             goto invalid_arg;
@@ -192,2 +192,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg2, size)) {
+        if (!ksu_access_ok(arg2, size)) {
             goto invalid_arg;
@@ -207,2 +207,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg2, len)) {
+        if (!ksu_access_ok(arg2, len)) {
             goto invalid_arg;
@@ -225,2 +225,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg1, sizeof(kpm_name))) {
+        if (!ksu_access_ok(arg1, sizeof(kpm_name))) {
             goto invalid_arg;
@@ -229,2 +229,2 @@ noinline int sukisu_handle_kpm(unsigned long control_code, unsigned long arg1,
-        if (!access_ok(arg2, sizeof(kpm_args))) {
+        if (!ksu_access_ok(arg2, sizeof(kpm_args))) {
             goto invalid_arg;
@@ -289,2 +289,2 @@ int do_kpm(void __user *arg)
-    if (!access_ok(cmd.control_code, sizeof(int))) {
+    if (!ksu_access_ok(cmd.control_code, sizeof(int))) {
         pr_err("kpm: invalid control_code pointer %px\n",
@@ -295,2 +295,2 @@ int do_kpm(void __user *arg)
-    if (!access_ok(cmd.result_code, sizeof(int))) {
+    if (!ksu_access_ok(cmd.result_code, sizeof(int))) {
         pr_err("kpm: invalid result_code pointer %px\n",
